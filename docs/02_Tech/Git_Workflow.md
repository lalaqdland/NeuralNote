# Git 工作流程规范

> NeuralNote 项目的 Git 分支管理和版本控制规范

---

## 📋 目录

- [分支策略](#分支策略)
- [工作流程](#工作流程)
- [提交规范](#提交规范)
- [常用命令](#常用命令)
- [注意事项](#注意事项)

---

## 🌿 分支策略

### 分支说明

| 分支 | 用途 | 位置 | 推送到 GitHub |
|------|------|------|--------------|
| **master** | 稳定版本，可发布 | 本地 + 远程 | ✅ 是 |
| **dev** | 开发分支，日常开发 | 仅本地 | ❌ 否 |

### 设计理念

**为什么只推送 master？**

1. **保持远程仓库简洁**
   - GitHub 上只有稳定的 master 分支
   - 避免开发中的不稳定代码暴露在远程仓库

2. **本地灵活开发**
   - dev 分支可以随意提交、修改、回滚
   - 不用担心影响远程仓库

3. **清晰的版本管理**
   - master 始终是可发布的稳定版本
   - 每次推送到 GitHub 的都是经过测试的代码

4. **避免混淆**
   - 团队成员或开源贡献者不会误用开发中的分支
   - 降低协作复杂度

---

## 🔄 工作流程

### 完整开发流程

```
┌─────────────────────────────────────────────────────────┐
│  1. 在本地 dev 分支开发                                    │
│     git checkout dev                                    │
│     (编写代码、测试...)                                    │
│     git add .                                           │
│     git commit -m "feat: 实现某功能"                      │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  2. 功能开发完成，切换到 master                            │
│     git checkout master                                 │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  3. 合并 dev 到 master（保留合并历史）                      │
│     git merge dev --no-ff -m "Merge: 功能描述"           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  4. 推送 master 到 GitHub                                │
│     git push origin master                              │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  5. 继续开发，切回 dev                                     │
│     git checkout dev                                    │
└─────────────────────────────────────────────────────────┘
```

### 详细步骤说明

#### 步骤 1：在 dev 分支开发

```bash
# 确保在 dev 分支
git checkout dev

# 查看当前状态
git status

# 编写代码...

# 添加所有更改
git add .

# 提交（使用规范的提交信息）
git commit -m "feat: 实现文件上传功能"

# 可以多次提交
git commit -m "fix: 修复上传文件大小限制问题"
git commit -m "docs: 更新 API 文档"
```

#### 步骤 2：切换到 master

```bash
# 切换到 master 分支
git checkout master

# 确认当前在 master 分支
git branch
# 输出：* master
#       dev
```

#### 步骤 3：合并 dev 到 master

```bash
# 使用 --no-ff 保留合并历史
git merge dev --no-ff -m "Merge branch 'dev': 文件上传功能

✨ 新增功能：
- 文件上传服务
- 本地存储和 OSS 支持
- 文件验证和大小限制

📝 API 接口：
- POST /api/v1/files/upload
- GET /api/v1/files/list
- DELETE /api/v1/files/{file_id}

🔧 技术实现：
- 统一存储接口
- 自动切换本地/OSS
- 完整的错误处理"
```

**为什么使用 `--no-ff`？**
- 保留完整的分支合并历史
- 可以清楚地看到哪些提交是从 dev 合并过来的
- 方便回滚整个功能

#### 步骤 4：推送到 GitHub

```bash
# 推送 master 分支到远程仓库
git push origin master

# 输出示例：
# To github.com:lalaqdland/NeuralNote.git
#    8e7a316..e2c4fca  master -> master
```

**⚠️ 注意**：
- ❌ **不要推送 dev 分支**：`git push origin dev`（禁止）
- ✅ **只推送 master 分支**：`git push origin master`（正确）

#### 步骤 5：继续开发

```bash
# 切回 dev 分支继续开发
git checkout dev

# 开始新功能的开发...
```

---

## 📝 提交规范

### Commit Message 格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

| Type | 说明 | 示例 |
|------|------|------|
| **feat** | 新功能 | `feat: 实现文件上传功能` |
| **fix** | Bug 修复 | `fix: 修复登录失败问题` |
| **docs** | 文档更新 | `docs: 更新 API 文档` |
| **style** | 代码格式（不影响功能） | `style: 格式化代码` |
| **refactor** | 重构（不是新功能也不是修复） | `refactor: 重构文件存储服务` |
| **perf** | 性能优化 | `perf: 优化数据库查询` |
| **test** | 测试相关 | `test: 添加文件上传测试` |
| **chore** | 构建/工具相关 | `chore: 更新依赖包` |
| **revert** | 回滚 | `revert: 回滚文件上传功能` |

### 提交信息示例

#### 简单提交

```bash
git commit -m "feat: 实现用户注册功能"
git commit -m "fix: 修复密码加密问题"
git commit -m "docs: 更新 README"
```

#### 详细提交

```bash
git commit -m "feat: 实现文件上传、OCR识别和AI分析功能

✨ 新增功能：
- 文件上传服务（支持本地存储和阿里云OSS）
- 百度OCR识别服务（通用文字识别+高精度版）
- AI分析服务（DeepSeek + OpenAI GPT-4）
- 完整工作流API（上传→OCR→AI→创建节点）

📝 API接口：
- 文件上传：5个接口（上传、列表、详情、删除、批量删除）
- OCR识别：2个接口（识别图片、批量识别）
- AI分析：4个接口（分析文本、生成摘要、提取关键词、完整工作流）

🔧 技术实现：
- 智能引擎选择（DeepSeek优先，降低成本）
- Token缓存机制（百度OCR，30天有效期）
- 统一存储接口（本地/OSS自动切换）
- 向量嵌入支持（1536维度）

📚 文档更新：
- 更新TODO.md进度（40%完成）
- 记录技术决策到DevLog
- 添加配置说明文档
- 更新NextDevPrompt开发提示

🔒 安全改进：
- 所有API密钥存储在.env文件
- .gitignore已配置，防止敏感信息泄露
- 文档中使用占位符，不暴露真实密钥"
```

---

## 🛠️ 常用命令

### 分支操作

```bash
# 查看所有分支
git branch

# 查看本地和远程分支
git branch -a

# 切换分支
git checkout dev
git checkout master

# 创建并切换到新分支（如果需要）
git checkout -b feature/new-feature
```

### 提交操作

```bash
# 查看状态
git status

# 查看修改内容
git diff

# 添加文件
git add .                    # 添加所有文件
git add src/backend/app/     # 添加指定目录
git add file.py              # 添加指定文件

# 提交
git commit -m "提交信息"

# 修改最后一次提交
git commit --amend
```

### 合并操作

```bash
# 合并 dev 到当前分支（保留合并历史）
git merge dev --no-ff -m "Merge: 功能描述"

# 查看合并历史
git log --graph --oneline --all
```

### 推送操作

```bash
# 推送 master 到远程
git push origin master

# 查看远程仓库
git remote -v

# 查看远程分支
git branch -r
```

### 查看历史

```bash
# 查看提交历史
git log

# 查看简洁历史
git log --oneline

# 查看图形化历史
git log --graph --oneline --all

# 查看某个文件的历史
git log -- path/to/file
```

### 撤销操作

```bash
# 撤销工作区的修改（未 add）
git checkout -- file.py

# 撤销暂存区的修改（已 add，未 commit）
git reset HEAD file.py

# 回滚到上一次提交（危险操作！）
git reset --hard HEAD^

# 查看所有操作记录（可以恢复误删的提交）
git reflog
```

---

## ⚠️ 注意事项

### 1. 永远不要推送 dev 分支

```bash
# ❌ 错误：推送 dev 分支
git push origin dev

# ✅ 正确：只推送 master 分支
git push origin master
```

**如果不小心推送了 dev 分支，删除远程 dev 分支**：

```bash
git push origin --delete dev
```

### 2. 合并前确保代码已测试

在合并到 master 之前，确保：
- ✅ 代码已经过测试
- ✅ 没有明显的 Bug
- ✅ 功能完整可用
- ✅ 文档已更新

### 3. 使用 --no-ff 合并

```bash
# ✅ 推荐：保留合并历史
git merge dev --no-ff -m "Merge: 功能描述"

# ❌ 不推荐：快进合并（丢失分支信息）
git merge dev
```

### 4. 定期同步 master 到 dev

如果在 master 上有紧急修复，记得同步到 dev：

```bash
# 在 master 上修复 Bug
git checkout master
git commit -m "fix: 紧急修复"
git push origin master

# 同步到 dev
git checkout dev
git merge master
```

### 5. 提交前检查状态

```bash
# 查看哪些文件被修改
git status

# 查看具体修改内容
git diff

# 确认无误后再提交
git add .
git commit -m "..."
```

### 6. 不要提交敏感信息

确保 `.gitignore` 已配置：

```gitignore
# 环境变量（包含 API 密钥）
.env
.env.local
.env.*.local

# 数据库文件
*.db
*.sqlite

# 上传的文件
uploads/

# IDE 配置
.vscode/
.idea/
```

### 7. 提交信息要清晰

```bash
# ❌ 不好的提交信息
git commit -m "修改"
git commit -m "update"
git commit -m "fix bug"

# ✅ 好的提交信息
git commit -m "feat: 实现文件上传功能"
git commit -m "fix: 修复登录失败问题"
git commit -m "docs: 更新 API 文档"
```

---

## 🎯 快速参考

### 日常开发流程

```bash
# 1. 开始开发
git checkout dev

# 2. 编写代码...

# 3. 提交
git add .
git commit -m "feat: 实现某功能"

# 4. 功能完成，合并到 master
git checkout master
git merge dev --no-ff -m "Merge: 功能描述"

# 5. 推送到 GitHub
git push origin master

# 6. 继续开发
git checkout dev
```

### 紧急修复流程

```bash
# 1. 在 master 上修复
git checkout master
git commit -m "fix: 紧急修复"
git push origin master

# 2. 同步到 dev
git checkout dev
git merge master
```

---

## 📚 相关文档

- [API 设计文档](./API_Design.md)
- [数据库配置文档](./Database_Setup.md)
- [开发日志](../03_Logs/DevLog.md)
- [开发任务清单](../../TODO.md)

---

## 🔗 参考资料

- [Git 官方文档](https://git-scm.com/doc)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Git 分支管理策略](https://nvie.com/posts/a-successful-git-branching-model/)

---

*本文档由 NeuralNote 开发团队维护*  
*最后更新：2026-01-31*

